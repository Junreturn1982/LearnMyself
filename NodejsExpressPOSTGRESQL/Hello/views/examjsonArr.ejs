<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Document</title>
</head>
<body>
	<% namsinh.forEach(function(item){ %>
		<h3><%= item %></h3>
	<% }) %>

	<% person.forEach(function(item){ %>
		<p><%= item.ho %> <%= item.ten %></p>
	<% }) %>
</body>
</html>